[
  { "id": "browse-home", "label": "Browse Home Page", "business": { "summary": "Fetches currencies, products, and cart. Converts prices to user's currency. Selects a random ad.", "rules": ["Prices converted to user's currency", "Random ad displayed (non-fatal)"], "preconditions": [], "outcomes": ["Home page rendered"], "failure_modes": [{ "condition": "CurrencyService or ProductCatalogService unavailable", "response": "500 error" }], "domain_events_emitted": [] }, "tags": ["read-only"] },
  { "id": "view-product", "label": "View Product", "business": { "summary": "Fetches product by ID, converts price, gets recommendations and contextual ad.", "rules": ["Recommendations and ads are non-fatal"], "preconditions": ["Product ID in URL"], "outcomes": ["Product page rendered"], "failure_modes": [{ "condition": "Product not found", "response": "500 error" }], "domain_events_emitted": [] }, "tags": ["read-only"] },
  { "id": "view-cart", "label": "View Cart", "business": { "summary": "Shows cart items with prices, shipping cost, and total in user's currency.", "rules": ["Each item price fetched and converted", "Shipping added to total"], "preconditions": [], "outcomes": ["Cart page rendered"], "failure_modes": [{ "condition": "Cart or product lookup fails", "response": "500 error" }], "domain_events_emitted": [] }, "tags": ["read-only"] },
  { "id": "add-to-cart", "label": "Add to Cart", "business": { "summary": "Validates product_id and quantity, verifies product exists, adds to cart, redirects to /cart.", "rules": ["Product existence verified before adding", "Redirects to /cart on success"], "preconditions": ["Form has product_id and quantity"], "outcomes": ["Item added, redirect to cart"], "failure_modes": [{ "condition": "Validation fails", "response": "422 error" }], "domain_events_emitted": [] }, "tags": ["writes"] },
  { "id": "set-currency", "label": "Set Currency", "business": { "summary": "Sets currency cookie and redirects back.", "rules": ["Cookie set with 48h max age"], "preconditions": ["Form has currency_code"], "outcomes": ["Cookie set, redirect"], "failure_modes": [{ "condition": "Invalid currency", "response": "422 error" }], "domain_events_emitted": [] }, "tags": ["writes"] },
  { "id": "place-order", "label": "Place Order", "business": { "summary": "Collects checkout form data, validates, calls CheckoutService/PlaceOrder, renders confirmation page.", "rules": ["All fields validated before submission"], "preconditions": ["Checkout form submitted"], "outcomes": ["Order placed, confirmation shown"], "failure_modes": [{ "condition": "Validation fails", "response": "422 error" }, { "condition": "CheckoutService fails", "response": "500 error" }], "domain_events_emitted": [] }, "tags": ["writes", "pci-scope"] },
  { "id": "empty-cart", "label": "Empty Cart", "business": { "summary": "Empties the user's shopping cart and redirects to home page.", "rules": ["Cart emptied via CartService/EmptyCart"], "preconditions": [], "outcomes": ["Cart emptied, redirect to home"], "failure_modes": [{ "condition": "CartService unavailable", "response": "500 error" }], "domain_events_emitted": [] }, "tags": ["writes"] },
  { "id": "logout", "label": "Logout", "business": { "summary": "Clears session and currency cookies, redirects to home page.", "rules": ["Session cookie cleared", "Currency cookie cleared"], "preconditions": [], "outcomes": ["Session ended, redirect to home"], "failure_modes": [], "domain_events_emitted": [] }, "tags": ["writes"] }
]
